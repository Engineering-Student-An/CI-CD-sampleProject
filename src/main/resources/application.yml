spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://aws-freetier.cj02wwkcqtgw.ap-northeast-2.rds.amazonaws.com:3306/cicdDB
    username: root
    password: justiff0801

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul
  # JDBC TimeZone Setting

server:
  port: 8080
